name: Sync Fork

on:
  schedule:
    - cron: '0 3 * * *'  # 03:00 UTC    
  workflow_dispatch: # on button click

jobs:
  sync:

    runs-on: ubuntu-latest

    steps:
      - uses: tgymnich/fork-sync@v2.0
        with:
          owner: exrom
          base: main
          head: main
